# Claude memory files (kept locally, never committed)
memory/
.claude-memory/

# Local Claude Code settings (machine-specific paths / permissions)
.claude/settings.local.json

# Compiled GSettings schema (regenerated by `make schemas`)
gnomepong@vanvonvan.github.io/schemas/gschemas.compiled

# Source art for assets (keep the big originals out of the repo)
*.psd
*.jpg
*.jpeg